# 开发计划

## 2026/5/19

- ~~增加华为对象存储接口~~

## 推荐后续开发顺序

### ~~第一阶段：架构展示增强~~

1. 实现 cloud-api 横向扩展能力，添加 nginx 网关
   - 去掉 cloud-api 固定 `container_name` 和宿主机端口绑定。
   - 新增 nginx 作为统一入口，转发前端 API 请求和 edge-node 上传请求。
   - 支持多个 cloud-api 副本共同对外服务。

2. 增加 Redis 作为 MySQL 缓存层
   - 先缓存 `/stats`、`/tasks/status`、`/devices`。
   - 使用短 TTL，降低 MySQL 高频统计和列表查询压力。
   - 暂时不优先缓存 `/images`，避免分页、筛选、排序带来的复杂失效问题。

### 第二阶段：业务能力增强

3. 添加 AI 分类接口，使用简单模型和动物数据集
   - 在现有 ai-service 基础上替换或扩展规则分类逻辑。
   - worker 与 ai-service 的 gRPC 通信保持不变。
   - 优先保证能完成动物图片分类演示。

4. 添加 MQTT 通信协议
   - 让端侧设备与 edge-node 之间可以使用 MQTT 上传数据。
   - edge-node 新增 MQTT 订阅入口，并复用现有 HTTP 上传后的转发、缓存、去重逻辑。
   - 先支持图片上传，后续再扩展到其他数据类型。

### 第三阶段：消息队列架构升级

5. 替换 Redis Stream 为 RabbitMQ
   - cloud-api 从 `XADD` 改为向 RabbitMQ publish 图片处理任务。
   - worker 从 Redis consumer group 改为 RabbitMQ consumer。
   - 增加 ack/nack、重试和失败处理策略。
   - Redis 职责逐步转为缓存层。

### 第四阶段：多媒体数据扩展

6. 支持图片之外的数据类型，例如文本文件和音频，暂不考虑视频
   - 将当前以 `images` 为核心的数据模型扩展为更通用的媒体/文件资产模型。
   - 上传接口、存储 key、worker 处理流程按数据类型分支。
   - 前端增加文本、音频等不同数据类型的展示方式。
